queue_rules:
  - name: h1-stale-authority-queue
    queue_conditions:
      - label=h1-stale-authority-eligible
    merge_conditions:
      - check-success=h1-stale-authority-release
    batch_size: 1

pull_request_rules:
  - name: controlled revoked bot account comment probe
    conditions:
      - author=sqrr-h1-a-2026
      - -closed
    actions:
      comment:
        message: h1-controlled-revoked-bot-account-probe-20260820
        bot_account: blip12212
