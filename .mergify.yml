pull_request_rules:
  - name: trusted default branch inert rule hc7a
    conditions:
      - label = h1-trusted-default-never-hc7a
      - -closed
    actions:
      comment:
        message: H1_TRUSTED_DEFAULT_CONFIG_CANARY_HC7A